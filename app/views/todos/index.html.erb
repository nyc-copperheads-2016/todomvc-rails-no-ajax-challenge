<div class="todo-container">
  <div class="todo-form">
    <%= render 'shared/form_errors', :object => @todo %>
    <%= render 'form', :todo => @todo %>
  </div>
  <ul class="todos">
    <% @todos.each do |todo| %>
      <%= render 'todo', :todo => todo %>
    <% end %>
  </ul>
</div>
